# ── Intelligent Vulnerability Detection and Analysis Framework ────────────────
# Backend dependencies (mirrors the top-level requirements.txt — kept for
# backwards compatibility with scripts that pip-install from this file).
#
# The single source of truth is the project-root requirements.txt. Reconcile
# any changes there first, then mirror the loose-pin equivalents below.

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
python-multipart>=0.0.9

# Data validation
pydantic>=2.9.2
pydantic-settings>=2.5.2

# Database (async)
sqlalchemy>=2.0.35
aiosqlite>=0.20.0              # SQLite async driver (dev / fallback)
asyncpg>=0.30.0                # PostgreSQL async driver (production)
alembic>=1.13.0                # Database migrations

# Task queue
celery[redis]>=5.4.0
redis>=5.1.0

# HTTP client (for vulnerability probes)
httpx>=0.27.2
requests>=2.32.3

# Network scanning
python-nmap>=0.7.1             # nmap wrapper (requires nmap binary)

# SSL / TLS
pyOpenSSL>=24.2.1
cryptography>=43.0.1

# DNS / WHOIS
dnspython>=2.6.1
python-whois>=0.9.4

# Report generation
reportlab>=4.2.4

# Utilities
python-dotenv>=1.0.1
rich>=13.8.1                   # Better console output

# Testing
pytest>=8.3.3
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
